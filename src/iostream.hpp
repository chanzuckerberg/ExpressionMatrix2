#ifndef CZI_EXPRESSION_MATRIX2_IOSTREAM_HPP
#define CZI_EXPRESSION_MATRIX2_IOSTREAM_HPP

// Add to the ExpressionMatrix2 namespace some names defined in namespace std.

#include <iostream>

namespace ChanZuckerberg {
    namespace ExpressionMatrix2 {
        using std::cin;
        using std::cerr;
        using std::cout;
        using std::dec;
        using std::endl;
        using std::flush;
        using std::hex;
        using std::istream;
        using std::ostream;
    }
}

#endif
